---
description: 코딩 효율성 및 원인 분석 중심 개발 가이드
alwaysApply: true
---

# 코딩 효율성 및 원인 분석 규칙

## 🎯 핵심 원칙

### 1. 원인 분석 우선 (절대 추가 금지 전 확인 필수)

#### ❌ 하지 말 것
```typescript
// 문제: 기능이 작동하지 않음
// 잘못된 접근: 바로 코드 추가
const toggleTheme = () => {
  setTheme(prev => prev === 'light' ? 'dark' : 'light');
  // 추가: useEffect도 추가함
  // 추가: 초기 마운트 useEffect도 추가함
  // 추가: 이중 안전장치도 추가함
};
```

#### ✅ 올바른 접근
```typescript
// 1단계: 원인 분석
// - setState는 비동기이므로 useEffect 실행 전까지 지연 가능
// - 사용자가 클릭 시 즉시 반영되지 않는 문제

// 2단계: 최소한의 수정으로 해결
const toggleTheme = () => {
  setTheme(prev => {
    const newTheme = prev === 'light' ? 'dark' : 'light';
    // 즉시 DOM 업데이트 (필요한 최소한의 수정)
    const root = document.documentElement;
    if (newTheme === 'dark') {
      root.classList.add('dark');
    } else {
      root.classList.remove('dark');
    }
    return newTheme;
  });
};
```

### 2. 코드 추가 전 체크리스트

#### 필수 확인사항
1. **원인 파악**: 왜 작동하지 않는가?
   - 기존 코드 로직 분석
   - React 생명주기 이해
   - 비동기 처리 확인
   - 상태 업데이트 타이밍 확인

2. **중복 확인**: 이미 같은 기능이 있는가?
   - 기존 useEffect 확인
   - 유사한 함수 확인
   - 라이브러리 기능 확인

3. **최소 수정**: 최소한의 변경으로 해결 가능한가?
   - 기존 코드 수정으로 해결
   - 새로운 코드 추가가 정말 필요한가?

4. **재사용 가능성**: 기존 함수/컴포넌트를 재사용할 수 있는가?

### 3. 코드 재사용 원칙

#### ❌ 하지 말 것
```typescript
// 중복 코드
const ProfilePage = () => {
  const [gender, setGender] = useState('');
  // 성별 버튼 JSX (50줄)
};

const LoginButton = () => {
  const [gender, setGender] = useState('');
  // 동일한 성별 버튼 JSX (50줄) - 중복!
};
```

#### ✅ 올바른 접근
```typescript
// 공통 컴포넌트 추출
const GenderSelector: React.FC<{
  value: string;
  onChange: (value: string) => void;
}> = ({ value, onChange }) => {
  return (
    <div className="flex gap-3">
      <button onClick={() => onChange('male')}>남성</button>
      <button onClick={() => onChange('female')}>여성</button>
    </div>
  );
};

// 재사용
<GenderSelector value={gender} onChange={setGender} />
```

### 4. 효율적인 코딩 패턴

#### 패턴 1: 단일 책임 원칙
```typescript
// ❌ 하나의 함수가 여러 일을 함
const handleSubmit = async () => {
  // 1. 검증
  // 2. API 호출
  // 3. 에러 처리
  // 4. 상태 업데이트
  // 5. 리다이렉트
};

// ✅ 각각 분리
const validate = () => { /* ... */ };
const submit = async () => { /* ... */ };
const handleError = (error) => { /* ... */ };
const handleSubmit = async () => {
  if (!validate()) return;
  try {
    await submit();
  } catch (e) {
    handleError(e);
  }
};
```

#### 패턴 2: DRY (Don't Repeat Yourself)
```typescript
// ❌ 반복되는 패턴
const fetchUserSessions = async () => {
  const userId = await getCurrentUserId();
  // ...
};

const fetchStats = async () => {
  const userId = await getCurrentUserId(); // 반복!
  // ...
};

// ✅ 공통 함수 추출
const withUser = async <T,>(fn: (userId: string) => Promise<T>) => {
  const userId = await getCurrentUserId();
  return fn(userId);
};
```

#### 패턴 3: 불필요한 상태 관리 제거
```typescript
// ❌ 불필요한 상태
const [isDark, setIsDark] = useState(false);
const [theme, setTheme] = useState('light'); // 중복!

// ✅ 하나의 상태로 충분
const [theme, setTheme] = useState<'light' | 'dark'>('light');
const isDark = theme === 'dark';
```

### 5. 코드 수정 전후 비교

#### 수정 전 분석
```typescript
// 문제: 무엇이 작동하지 않는가?
// 원인: 왜 작동하지 않는가?
// 해결: 최소한의 수정으로 어떻게 해결할 수 있는가?
```

#### 수정 후 검증
```typescript
// 추가한 코드가 정말 필요한가?
// 기존 코드로 충분하지 않은가?
// 중복이 생기지 않았는가?
```

### 6. 구체적인 금지 사항

#### 절대 하지 말 것
1. **"이중 안전장치"라는 이름으로 중복 코드 추가**
   ```typescript
   // ❌
   useEffect(() => { /* ... */ }, [theme]);
   useEffect(() => { /* 같은 로직 */ }, []); // 불필요!
   ```

2. **기존 코드 확인 없이 새 기능 추가**
   ```typescript
   // ❌ 새로운 함수 추가
   const newFunction = () => { /* ... */ };
   // 이미 비슷한 함수가 있었는데 확인 안 함
   ```

3. **"일단 작동하게 만들자"는 마인드로 임시 코드 추가**
   ```typescript
   // ❌
   // TODO: 나중에 수정
   // 임시 코드
   if (true) { /* ... */ }
   ```

4. **동일한 로직을 여러 곳에 복사**
   ```typescript
   // ❌ 컴포넌트 A에 복사
   // ❌ 컴포넌트 B에 복사
   // ❌ 컴포넌트 C에 복사
   ```

### 7. 코드 리뷰 체크리스트

#### 코드 작성 전
- [ ] 원인을 명확히 파악했는가?
- [ ] 기존 코드를 충분히 확인했는가?
- [ ] 중복 코드가 있는지 확인했는가?
- [ ] 재사용 가능한 코드가 있는지 확인했는가?

#### 코드 작성 중
- [ ] 최소한의 수정으로 해결 가능한가?
- [ ] 불필요한 상태/함수를 추가하지 않았는가?
- [ ] 기존 패턴을 따르고 있는가?

#### 코드 작성 후
- [ ] 추가한 코드가 정말 필요한가?
- [ ] 중복이 생기지 않았는가?
- [ ] 다른 곳에서 재사용 가능한가?

### 8. 예시: 좋은 수정 vs 나쁜 수정

#### 나쁜 수정 예시
```typescript
// 문제: 테마 토글이 작동하지 않음
// 나쁜 접근: 여러 useEffect 추가
useEffect(() => { /* ... */ }, [theme]);
useEffect(() => { /* 같은 로직 */ }, []); // 중복
useEffect(() => { /* 또 같은 로직 */ }, [theme, saved]); // 불필요

const toggleTheme = () => {
  setTheme(prev => prev === 'light' ? 'dark' : 'light');
  // 추가 로직 없음
};
```

#### 좋은 수정 예시
```typescript
// 문제: 테마 토글이 작동하지 않음
// 원인 분석: setState는 비동기, useEffect 실행 전까지 지연
// 좋은 접근: toggleTheme에서 즉시 DOM 업데이트
const toggleTheme = () => {
  setTheme(prev => {
    const newTheme = prev === 'light' ? 'dark' : 'light';
    // 즉시 DOM 업데이트 (필요한 최소한의 수정)
    document.documentElement.classList.toggle('dark', newTheme === 'dark');
    return newTheme;
  });
};

useEffect(() => { /* 상태 동기화 */ }, [theme]); // 유지
```

## 🎯 핵심 메시지

1. **원인 분석 먼저**: 문제가 무엇인지, 왜 발생하는지 파악
2. **최소 수정**: 기존 코드를 최대한 활용, 최소한의 변경
3. **재사용 우선**: 새로 만들기 전에 기존 코드 재사용 가능한지 확인
4. **중복 제거**: 같은 로직은 한 곳에만 존재
5. **불필요한 코드 금지**: "이중 안전장치" 같은 이름으로 중복 코드 추가 금지

## 📝 참고

- React 공식 문서: [Thinking in React](https://react.dev/learn/thinking-in-react)
- Clean Code 원칙: DRY, KISS, YAGNI
- Refactoring: 기존 코드 개선 우선, 새 코드 추가는 최후의 수단
