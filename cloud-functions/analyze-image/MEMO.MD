1. 사용자 답안 및 실제 정답 추출안됨. 정오답 체크도 안되어있음. 아래 사항들을 확인하여 근본원인 도출하라.
    1.1. 클라우드 펑션 로그(쿼리하라)
    1.2. 슈파베이스 시간초과로 버렸지만 이미지 분석 만큼은 잘되던 버전(커밋명:'b6fd71be fix: WASM 뮤텍스 추가로 멀티이미지 병렬 크롭 시 상태 오염 방지, batch_size=3 원복')

2. 이미지 분석 완료 시 바로'/session'으로 이동됨. 이걸 추가하라고 한적이 없는데 왜 이런 동작을 하지? 사용자가 이미지를 올리면 즉시 /stats로 이동하고 사용자는 웹페이지를 나가는 등 의 자유롭게 행동 할 수 있어야함